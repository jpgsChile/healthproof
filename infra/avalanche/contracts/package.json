{
  "name": "@healthproof/infra-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run deploy/deploy.ts",
    "deploy:healthproof": "hardhat run scripts/deployHealthProof.ts",
    "deploy:healthproof:hygieia": "hardhat run scripts/deployHealthProof.ts --network hygieia",
    "deploy:healthproof:local": "hardhat run scripts/deployHealthProof.ts --network localhost",
    "deploy:hygieia": "hardhat run deploy/deploy.ts --network hygieia",
    "deploy:local": "hardhat run deploy/deploy.ts --network localhost",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ethers": "^6.13.0"
  }
}
